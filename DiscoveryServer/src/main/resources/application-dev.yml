server:
  port: 8761

spring:
  application:
    name: discovery-service

management:
  metrics:
    enable:
      processor: false

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false